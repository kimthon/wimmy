<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/Select</item>
        <item name="colorPrimaryDark">@color/Selected</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/loding</item>
    </style>

    <style name="TabTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="colorPrimary">#33B5E5</item>                                <!-- 배경 -->
        <item name="android:textColorPrimary">@android:color/holo_purple</item>       <!-- 탭바 선택된 텍스트 -->
        <item name="android:textColorSecondary">@android:color/black</item>     <!-- 탭바 텍스트 -->

        <item name="colorAccent">@android:color/holo_purple</item>                    <!-- 상단 움직이는 바 색-->
    </style>

    <style name="calendarTheme" parent="TextAppearance.MaterialCalendarWidget.Date">
        <item name="android:textColor">#EE88C9</item>
        <item name="android:background">#EE88C9</item>
        <item name="android:textSize">15pt</item>
    </style>
</resources>
